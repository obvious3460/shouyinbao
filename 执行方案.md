# 💰 收银宝 · 服务端版完整执行方案

> 本方案**完全自包含**：拿到任何一台电脑上都能照着实施，不需要依赖开发者、不需要外网、不需要 MySQL、不需要 npm。
> 只需要一台「长期开机的电脑」当服务器 + 一台装了 Node.js 的电脑（或同一台）。
> 实施时间约 **10 分钟**（不含下载 Node.js 的时间）。

---

## 0. 这套东西是什么

```
┌────────────┐   ┌────────────┐   ┌────────────┐
│ 手机(收银)  │   │ 手机(查账)  │   │ 电脑2(记账) │
└─────┬──────┘   └─────┬──────┘   └─────┬──────┘
      │    同一 Wi-Fi / 局域网，浏览器访问
      └────────────────┼────────────────┘
                       ▼
        ┌──────────────────────────────┐
        │  服务器电脑（长期开机）        │
        │  Node.js 内置 http 服务 :3000 │
        │  SQLite 真实数据库 .db 文件   │ ← 唯一数据源，事务保证不丢单
        └──────────────────────────────┘
```

- 电脑开机、启动服务后，**手机和电脑用浏览器就能同时收银、记账、查账**，数据实时写入服务器上的 SQLite 数据库文件。
- **零依赖**：只用到 Node.js 自带的模块（http / sqlite / fs），**不需要 npm install、不需要外网、不需要 MySQL**。
- 权限按**设备类型**区分（不用账号）：
  - 📱 **手机**：收银记录、出库入库、出账、查看流水/统计（不能改价格、不能进管理）
  - 💻 **电脑**：全部功能，含商品价格编辑、会员/等级管理、系统设置

---

## 1. 你需要准备什么

| 项 | 说明 |
|---|---|
| 服务器电脑 | 一台长期开机的电脑（台式机/旧笔记本都行，配置无要求） |
| Node.js | 在**服务器电脑**上安装，版本 ≥ 22.5（推荐 LTS v22 或 v24），下载地址：https://nodejs.org |
| 手机/其他电脑 | 任意，只需浏览器（Chrome/Edge/Safari） |

### 1.1 安装 Node.js

**Windows**：打开 https://nodejs.org → 下载 **LTS 版**（.msi）→ 双击安装，一路「下一步」即可（无需勾选额外选项）。

**macOS**：下载官网 .pkg 安装；或用 `brew install node`。

**Linux（Debian/Ubuntu）**：
```bash
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 1.2 验证安装

打开终端（Windows 按 `Win+R` 输入 `cmd` 回车），输入：

```bash
node -v
```

显示 `v22.x.x` 或更高（含 v24）即成功。**这步必须通过，否则服务起不来。**

---

## 2. 文件清单（把这整个文件夹拷过去）

把 `shouyinbao-server` **整个文件夹**复制到服务器电脑上（U 盘 / 网盘 / 微信文件传输均可）：

```
shouyinbao-server/
├── server.js            ← 服务端主程序（唯一需要运行的代码）
├── public/
│   ├── index.html       ← 网页界面
│   └── app.js           ← 前端逻辑（API 客户端）
├── 启动服务.bat          ← Windows 双击启动
├── 启动服务.sh           ← macOS / Linux 启动
├── 防火墙放行.bat        ← Windows 放行端口（需管理员）
├── data/                ← 首次启动自动生成，数据库文件在这
└── backup/              ← 首次启动自动生成，每日自动备份在这
```

---

## 3. 部署步骤（约 3 分钟）

### 第 1 步：确认 Node.js 已装好
见第 1.2 节，`node -v` 能输出版本号。

### 第 2 步：启动服务

**Windows**：双击 `启动服务.bat`
- 会弹出两个窗口：一个黑窗口（服务器，**别关它**），一个浏览器窗口（自动打开收银宝）
- 黑窗口里会打印类似：
  ```
  💰 收银宝服务端已启动
  本机访问:  http://127.0.0.1:3000
  局域网访问: http://192.168.1.26:3000   ← 手机用这个
  ```

**macOS / Linux**：
```bash
cd shouyinbao-server
chmod +x 启动服务.sh
./启动服务.sh
```

### 第 3 步：放行防火墙（仅 Windows 首次需要）

- 方式一（推荐）：双击 `防火墙放行.bat`，在弹窗提示时选「以管理员身份运行」，看到「已放行」即完成
- 方式二：启动服务时 Windows 会弹出防火墙询问，勾选「专用网络」后点「允许访问」即可

### 第 4 步：验证

服务器电脑浏览器打开 `http://127.0.0.1:3000`，看到收银宝界面即成功。

---

## 4. 手机 / 其他电脑访问

1. 手机连接**与服务器电脑同一个 Wi-Fi**
2. 手机浏览器输入服务器黑窗口打印的地址：`http://192.168.1.26:3000`（以你实际打印的为准）
3. 手机自动进入**手机模式**：只显示 概览 / 收银台 / 出库入库 / 出账 / 流水 / 统计
4. 其他电脑用同一方式访问，自动进入**电脑模式**（完整管理）
5. 小技巧：手机浏览器点「添加到主屏幕」，以后像 App 一样点开就用

> ⚠️ 电脑 IP 可能因路由器重启而变化。变了之后**重看服务器黑窗口**（启动时会重新打印当前地址）。想固定地址，可在路由器后台给电脑设置「静态 IP 保留」。

---

## 5. 功能与权限对照

| 功能 | 📱 手机 | 💻 电脑 |
|---|---|---|
| 收银结算（含 VIP 折扣/积分抵扣） | ✅ | ✅ |
| 出库入库（补货/损耗） | ✅ | ✅ |
| 出账记账（新增） | ✅ | ✅ |
| 出账编辑/删除 | ❌ 只读 | ✅ |
| 流水账本 / 统计 / 概览 | ✅ 查看 | ✅ |
| 报表中心（日报/周报/月报，打印/导出TXT） | ❌ | ✅ |
| 商品价格编辑 / 增删商品 | ❌ | ✅ |
| 会员 / VIP 等级管理 | ❌ | ✅ |
| 系统设置 / 数据备份 | ❌ | ✅ |

**折扣计算顺序**（服务端统一计算，多设备结果一致）：
```
小计 → 整单折扣 → VIP 额外折扣 → 积分抵扣 → 应收
例：¥100 打 9 折 + 金卡 92 折 + 500 积分(100分=1元)
    整单折扣 -¥10 → VIP 折扣 -¥7.2 → 积分抵扣 -¥5 → 实收 ¥77.8
```

---

## 6. 数据与备份（重要）

- **数据位置**：`shouyinbao-server/data/shouyinbao.db`（SQLite 真实数据库文件，断电/重启不丢）
- **自动备份**：服务器每天 **23:00** 自动复制一份到 `backup/` 文件夹，保留最近 7 份
- **手动备份**（随时）：电脑端「系统设置 → 下载数据库备份」，或直接复制 `data/shouyinbao.db` 文件
- **恢复**：先关掉服务窗口 → 用备份文件覆盖 `data/shouyinbao.db` → 重新启动服务
- **换电脑**：把 `data/shouyinbao.db` 一起拷走，放到新机器的同路径下即可，历史数据全在

---

## 7. 常见问题（FAQ）

| 问题 | 解决办法 |
|---|---|
| 双击 bat 提示找不到 node | Node.js 没装好，重看第 1 节，`node -v` 必须能出版本号 |
| 手机打不开网页 | ① 手机和服务器是否同一 Wi-Fi ② 防火墙是否放行 ③ 地址是否最新（重看服务器窗口）|
| 端口 3000 被占用 | 改端口：在服务器窗口先 `Ctrl+C` 停掉，然后运行 `set PORT=3001` 后再 `node server.js`，手机访问 `http://IP:3001` |
| 中文显示乱码 | Windows 终端右键标题栏 → 属性 → 字体选「新宋体/等线」；bat 已内置 `chcp 65001` |
| 电脑关机后手机连不上 | 正常，服务随电脑关机停止（这就是"电脑开启时能用"的设计）|
| 想开机自动启动服务 | 见第 8 节 |
| 在外面（不在同一 Wi-Fi）想访问 | 见第 8 节 Tailscale |

---

## 8. 可选增强

### 8.1 Windows 开机自动启动服务
把 `启动服务.bat` 的**快捷方式**放入开机启动文件夹：
1. 按 `Win+R` 输入 `shell:startup` 回车
2. 右键 `启动服务.bat` → 创建快捷方式 → 把快捷方式拖进打开的文件夹
下次开机自动启动。

### 8.2 远程访问（人在外面也能用）：Tailscale 异地组网
免费、加密、无需公网 IP，推荐方案：
1. 服务器电脑和手机都安装 Tailscale（https://tailscale.com，手机装 App）
2. 用同一账号登录，两台设备即组成虚拟局域网
3. 服务器控制台会多打印一行 Tailscale 虚拟 IP（100.x.x.x），手机在任何网络下访问 `http://100.x.x.x:3000` 即可
4. 其他电脑同理安装登录即可

---

## 9. 自测清单（部署完成后逐项打勾）

- [ ] 服务器黑窗口打印出「局域网访问 http://192.168.x.x:3000」
- [ ] 服务器电脑浏览器打开 `http://127.0.0.1:3000` 看到界面
- [ ] 手机浏览器打开局域网地址，左上角显示「📱 手机模式」
- [ ] 手机上收一笔钱：商品库存减少、流水出现、会员积分增加
- [ ] 手机上「商品管理 / 会员管理 / 系统设置」菜单不显示；直接手动输入网址也进不去（服务端拦截）
- [ ] 电脑上修改一个商品价格成功，手机端收银显示新价格
- [ ] 电脑端「系统设置 → 下载数据库备份」能下载到 .db 文件
- [ ] `data/` 目录里能看到 `shouyinbao.db`

---

## 10. 技术说明（想深入/改造再看）

### 10.1 API 一览
| 接口 | 方法 | 说明 |
|---|---|---|
| /api/bootstrap | GET | 初始化数据（设置/商品/会员/等级 + 设备类型）|
| /api/sales | POST | 结算（事务：销售单+明细+扣库存+加积分）|
| /api/sales | GET | 销售列表（可按日期筛选）|
| /api/products · /api/members · /api/levels · /api/expenses | GET/POST/PUT/DELETE | 各业务增删改查（写操作电脑端）|
| /api/stock | POST | 出入库（手机+电脑）|
| /api/stock-moves | GET | 出入库记录 |
| /api/dashboard | GET | 首页概览 |
| /api/stats | GET | 统计（范围筛选）|
| /api/report | GET | 报表（type=daily/weekly/monthly&date=日期）|
| /api/settings | GET/PUT | 设置（PUT 电脑端）|
| /api/backup | GET | 下载数据库备份 |
| /api/reset-sample / /api/clear | POST | 恢复示例 / 清空（电脑端）|

### 10.2 数据表
`settings`(设置) · `levels`(VIP等级) · `products`(商品) · `members`(会员+积分) · `sales`(销售单) · `sale_items`(销售明细) · `expenses`(出账) · `stock_moves`(出入库)

### 10.3 以后想换 MySQL
本项目数据库操作集中在 `server.js` 顶部的建表/读写函数中，前端只调 `/api/*`。
将来若需多店/更高并发，把 `node:sqlite` 部分替换为 mysql2 驱动（需要外网装 npm 包），**前端与 API 完全不用改**。

---

## 11. 安全提醒
- 本系统面向**可信局域网**使用，未内置账号密码。请勿把端口 3000 直接暴露到公网。
- 需要公网访问时，使用第 8.2 节的 Tailscale（自带加密），不要做端口映射。
